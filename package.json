{
  "name": "gpt-image-2-mcp",
  "version": "1.0.0",
  "description": "MCP server for OpenAI GPT Image 2 generation and editing with prompt gallery",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "gpt-image-2-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/*.test.mjs",
    "prepare": "npm run build",
    "dev": "tsc --watch",
    "image:curl": "bash scripts/generate-image-curl.sh"
  },
  "keywords": [
    "mcp",
    "gpt-image-2",
    "openai",
    "image-generation",
    "text-to-image",
    "prompt-gallery"
  ],
  "author": "wuyoscar",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^16.0.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
